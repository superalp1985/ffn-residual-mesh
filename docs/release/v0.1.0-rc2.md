# ffn-residual-mesh

GitHub Release title: `ffn-residual-mesh`  
Suggested tag for this revision: `v0.1.0-rc5`

这是 `ffn-residual-mesh` 的第一个公开 release candidate。

它研究的不是“再做一个模型分片器”，而是一个更具体的硬件资源交换问题：**GPU 有计算能力，却经常等不到数据；主机内存、平板和旧手机有大量存储和串行计算能力，却无法直接参与 GPU 的高密度算子。**

本项目的做法是：

> **冷启动时把 FFN 权重预展开、分区和编译；运行时把臃肿的 base 留在主机/平板内存，只把紧凑 residual 搬到中心 GPU，再在非线性之前精确合并。**

简单说，就是用更多 GPU 算力和冷启动时间，换显存容量与内存/显存之间的带宽压力。

## 我们先解决的不是 FLOPs

现在显卡最容易被误解的一点是：峰值算力很高，不等于实际 FFN 计算密度很高。很多本地推理场景里，GPU 算子已经准备好了，却在等待：

- 权重从显存或主机内存搬过来；
- 显存容量不够，权重被反复分页；
- PCIe/H2D 带宽不足；
- 大量重复的 FFN 主项占据了最宝贵的数据通路；
- CPU、GPU 和同步点之间出现空转。

所以本项目不把“减少 FLOPs”作为第一目标。我们关注的是 **有效计算密度**：每搬一个字节，能让 GPU 连续做多少有用计算；GPU 有多少时间是真正在算，而不是等数据。

## 资源等价交换

算法把资源重新分配：

```text
投入：冷启动时间 + 主机/平板/手机内存 + CPU/设备计算
换取：更少的动态权重搬运 + 更低的显存压力 + 更少的 GPU 等待
```

具体对应关系是：

| 原本的瓶颈 | 算法的交换方式 |
|---|---|
| FFN base 权重太大 | 冷启动预展开，长期放在 RAM/worker 内存 |
| GPU 等待权重流 | GPU 只接收 residual tile，并持续做规则计算 |
| VRAM 放不下更大 dense 模型 | 按层、按块驻留，base 不强制常驻显存 |
| 主机 CPU/L3 带宽不足 | 用多线程、平板或旧手机组成 base worker 池 |
| 大量小搬运和同步 | 预取、超级块、双缓冲和 deadline fallback |

这不是凭空减少计算，也不是把 FLOPs 藏起来。**我们承认计算量可能增加，但用可利用的 GPU 算力换取带宽和显存空间。**只要 GPU 等待下降、copy/compute overlap 上升、有效计算密度提高，这个交换就是值得的。

## 这次 Release 的核心

- 分层 FFN 拆分/合并算法
- 量化整数域中的精确重建路径
- CPU/主机 base + GPU residual 的 CUDA 桥接实验
- MiniMax H3 + ComfyUI 手机/平板集群模拟
- TeaCache real/reuse step 账本
- 手机/平板 worker 的 loopback 协议原型
- Qwen3.8-27B-class 的显存和带宽预算

重点不是减少 FLOPs。重点是让每一个传到 GPU 的字节触发更多连续计算，让 GPU 少等数据。

## 算法怎么来的

标准 gated FFN 可以写成：

```text
g = W_gate x
u = W_up   x
h = SiLU(g) * u
y = W_down h
```

我们把每层权重拆成两部分：

```text
W = B + R
```

其中：

- `B` 是冷启动时预展开的 base，适合放在主机 RAM 或平板内存；
- `R` 是不规则 residual，集中交给 GPU 做高密度计算。

于是：

```text
g = Eval(B_gate, x) + Eval(R_gate, x)
u = Eval(B_up,   x) + Eval(R_up,   x)
h = SiLU(g) * u
y = W_down h
```

注意，gate 和 up 必须在进入 SiLU 之前合并。不能先分别激活再相加，否则一般不等价。

## 为什么拆分可以保持精度

最直观的例子是：

```text
(a + b) * (c + d)
= ac + ad + bc + bd
```

这里 `a`、`c` 可以代表规律性强、适合预展开的主项，`b`、`d` 代表需要精确处理的细节。真正的实现不能把交叉项偷偷丢掉；它们要么进入 base 公式，要么进入 residual 计算。

在量化整数域里，同样的规则成立。以一组量化权重为例：

```text
q[i] = C[index[i]] + r[i]
```

点积可以写成：

```text
sum(x[i] * q[i])
= sum(C[index[i]] * x[i])
  + sum(x[i] * r[i])
```

第一项是有限状态的基项聚合，第二项是完整 residual。只要 residual 不被裁剪，合并结果就能在选定的整数/量化域内严格重建原始点积。允许误差的路径另行测量，并且保留中心 GPU 精确 fallback。

这也是本项目和普通“近似压缩”的区别：**核心 exact 路径不是把重要数字删掉，而是改变它们的存放位置和计算时机。**

## 运行时数据流

```text
冷启动：原始量化权重
    -> 完整扫描一次
    -> 编译 base 公式、部分和、residual tile、scale、索引
    -> 主机 RAM / 平板 worker 常驻

运行时：输入 tile
    -> CPU/worker 生成 base
    -> GPU 接收 residual tile
    -> 合并 gate/up
    -> 原始 SwiGLU
    -> down、attention、decode
```

正常运行不能再次顺序扫描等价的稠密主项权重流。否则只是换了一个名字，带宽问题并没有解决。

## 手机和平板集群的应用前景

第一阶段不追求让手机代替 GPU，而是让它们成为 FFN base worker：

- 主机 RTX 4070 保留 GPU residual、SwiGLU、down 和 Attention；
- 平板或旧手机保存编译后的 FFN base 分片；
- 中心端广播输入 descriptor；
- worker 并发返回 base tile；
- 任意 worker 超时或校验失败，立即回退中心 GPU 精确路径。

最适合的硬件顺序是：

1. 同机 loopback 多进程
2. USB/USB-Ethernet 平板
3. 10GbE 平板小集群
4. 有线手机集群
5. 普通 Wi-Fi 手机集群

对 MiniMax H3 的模拟表明，精确 gate/up 回传在 1 Gb/s 下约为 839.8 MiB/real step，网络会成为关键路径；在 10 Gb/s 下，worker 分支可以隐藏在 GPU 分支之后。这个结果支持先做有线平板，而不是先堆几十台 Wi-Fi 手机。

## ComfyUI 方向

目标不是把 ComfyUI 工作流拆成很多 RPC 节点，而是提供一个可选的模型 backend：

```text
ComfyUI
  -> TeaCache 判断 real/reuse step
  -> FFN worker backend
  -> GPU residual + merge
  -> 原有 SwiGLU/down/SageAttention 路径
```

TeaCache 的 reuse step 不需要重新调用手机 worker，因此可以直接减少集群流量。MiniMax H3 是当前最合适的验证对象，后续可以扩展到更大的 dense 模型和 27B-class 语言模型。

## 当前证据

仓库目前已经公开：

- 14 个 Python/unittest 测试，覆盖整数重建、残差打包、协议校验和 deadline fallback；
- `compileall` 通过；
- layer-level CPU base + GPU residual CUDA 实验；
- MiniMax H3/ComfyUI 分析模拟；
- 10GbE/USB 平板方向的部署设计；
- Qwen3.8-27B-class 显存与 H2D 预算脚本。

这些结果证明了算法和调度方向有继续做的价值，但还不等于已经完成 Android 实机或 27B 端到端 benchmark。

## 复现实验

```powershell
python -m unittest discover -s tests -p "test*.py" -v
python -m compileall -q scripts src tests
python scripts/simulate_comfyui_phone_ffn.py `
  --network-gbps 10 `
  --phone-return gate_up_exact `
  --out results/comfyui_h3_phone_exact_10gbps.json
```

完整数学说明见：

- `docs/math_principles_report.md`
- `docs/weight_code_split_spec.md`
- `docs/comfyui_phone_cluster_design.md`
- `docs/release/ffn_residual_mesh_math_note_bingqin_wang.md`（Bingqin WANG 署名的推导小文章）

## 还没有宣称什么

- 没有宣称 27B 已经在 8 GiB 显卡上端到端跑通；
- 没有宣称手机数量越多就一定越快；
- 没有把 hidden 近似路径当成 exact；
- 没有把模拟结果当成 Android 实测；
- 没有替代 TeaCache、SageAttention 或 ComfyUI 的精确 fallback。

## 下一步

下一版优先完成：

- USB/10GbE 平板 worker；
- 真实 payload、延迟、温度和丢包统计；
- ComfyUI 可选 backend；
- H2D bytes、GPU wait、copy/compute overlap、显存峰值和 fallback rate 的实时面板；
- 真实 MiniMax H3 视频工作流验证。

如果你有闲置 Android 手机、平板、迷你主机或 10GbE 网卡，欢迎贡献设备测试数据。这个项目的目标不是证明某一台机器很强，而是把一堆闲置设备组织成一个能被测量、能回退、能复现的 FFN 计算网络。

## License

MIT License。
