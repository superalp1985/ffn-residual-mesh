# Iteration 57: Persistent Tiled Full FFN

Date: 2026-09-05

## Full FFN Integration

`TiledResidentGateUp` now feeds the original quantized down projection after
GPU-side gate/up merge and SwiGLU. The row-tiled residual path remains exact
for the compiled Q4_K gate/up artifact.

Qwen3.8-27B layer 3, real artifact and IQ4_NL down projection:

- cold wall: `227.27 ms`;
- warm wall: `4.20 ms`;
- warm CPU base: `1.08 ms`;
- warm gate/up tile residual kernel: `1.17 ms`;
- warm down projection: `0.97 ms`;
- gate/up residual package resident: `111,411,200 bytes`;
- down raw package: `50,135,040 bytes`;
- repeated-run weight H2D: no increase after cold start.

The cold run includes Triton compilation, tile initialization, and resident
package upload. The warm run is the relevant steady-state signal for this
experiment.

## Scope

This is a single-layer FFN measurement. It excludes attention, KV cache,
layer-to-layer scheduling, tokenizer/runtime overhead, and end-to-end token
generation. It therefore cannot support a 27B tokens-per-second claim.

## Next Bottleneck

The core persistent-residual path is now complete for one mixed-format layer.
The next engineering step is selecting a persistent layer subset under the
8 GB budget and measuring a multi-layer decode trace with prefetch deadlines.
