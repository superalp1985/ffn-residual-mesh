# 2026-09-02 Iteration 2

## 研究前置

检索了 LUT-LLM、FLUTE、TARDIS。结论是：向量中心查找、离线权重重排、分段线性近似和离群回退均已有先例；本轮不重复造通用 LUT 内核，验证它们在 Qwen3.5 dense SiLU FFN 和 CPU/GPU 混合路径上的适配边界。

## 工程变更

- 修复 `ffn_probe` 的 JSON 路径输出，改用 `generic_string()`。
- 捕获 `attn_post_norm-{layer}`，即 Qwen3.5 FFN 的真实输入。
- 增加 16 组多主题校准提示和可复现批量捕获脚本。
- 增加共享低秩残差分析，使用样本维度双空间 SVD，避免重复构造 2048x2048 分解。

## 结果

数据：16 组提示，24 层，约 275 个 token/层（固定抽样上限 384 未触发）。

跨主题 `ffn_out` 近似的平均相对 token-L2：

| K | 纯中心查表 | 共享 rank=32 | 共享 rank=64 | 共享 rank=128 |
|---:|---:|---:|---:|---:|
| 1 | 0.915 | 0.795 | 0.722 | 0.580 |
| 4 | 0.867 | 0.743 | 0.672 | 0.531 |
| 16 | 0.768 | 0.667 | 0.597 | 0.457 |
| 64 | 0.601 | 0.514 | 0.445 | 0.302 |

## 判断

全模型小表不可用；`K=64 + rank=128` 已显示局部线性结构，但误差仍不能直接用于端到端替换。下一轮应优先：

1. 按层选择，先只替换误差低的后层（例如 layer 23、22、18）。
2. 比较输入路由与 `ffn_swiglu` 路由；后者可能更贴近 `ffn_down` 的线性结构。
3. 测试加性分块 LUT，验证 CPU 只做量化和向量累加能否替代部分矩阵搬运。
4. 增加留出提示集，避免只报告校准集内误差。

原始结果：`results/cluster_residual_20260902_k64.json`。
