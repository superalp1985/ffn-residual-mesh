# 贡献指南

本仓库是实验性研究代码。贡献应优先增加可复现证据，而不是只增加理论描述。

## 提交前要求

1. 在仓库根目录运行相关脚本或测试，并记录硬件、CUDA、模型量化格式和关键参数。
2. 不提交模型权重、GGUF、safetensors、DLL、EXE、results 或本地运行库。
3. 数学改动同时更新 docs/math_principles_report.md 或 docs/weight_code_split_spec.md。
4. 任何近似必须说明误差来源、校准集、holdout 集和精确回退条件。
5. 性能结果必须报告 H2D bytes、GPU 等待、copy/compute overlap 和显存峰值，不能只报告 FLOPs 或压缩率。

## 实验记录

在 docs/log/ 新增按日期命名的记录，至少包含：假设、命令、设备、输入、结果、结论和下一步。脚本失败时应返回非零退出码。

## 代码风格

- Python 使用标准库优先，保持命令行参数明确。
- CUDA/C++ 改动应提供最小可编译路径。
- 不把查表、低秩、残差裁剪和精确算术拆分混写成同一结论。
- 不删除其他贡献者的实验日志或未相关改动。

## Pull request 内容

Pull request 描述应说明：

- 改动解决的具体问题；
- 运行的测试命令和结果；
- 与原始 FFN 的误差；
- 是否改变运行时权重访问边界；
- 是否仍有未验证假设。

